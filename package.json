{
  "name": "wrafjs",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter playground dev"
  },
  "dependencies": {
    "rimraf": "^6.1.3"
  },
  "devDependencies": {
    "jsdom": "^29.0.1"
  }
}
